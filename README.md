# phyloDNN
use ``train_and_test.py`` to run models. A variety of invariant nets, attention nets, and GNNs are defined in phyloformer/models.
plotting and visualization scripts and notebooks are in plotting/
the python requirements are defined in ``environment.yml``.
